<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.vuum.mocca"
    android:versionCode="1"
    android:versionName="1.0" >
    
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <!--
        Beginning with Android 4.4, reading or writing files in your app's private directories does not require
        the READ_EXTERNAL_STORAGE or WRITE_EXTERNAL_STORAGE permissions.
        So you can declare the permission should be requested only on the lower versions of Android

        For target sdk version for iRemember app (17) we need to have READ_EXTERNAL_STORAGE and WRITE_EXTERNAL_STORAGE permissions
        and it's a good practice to set the max required sdk version.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="18"/> <!-- Line 12 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="18"/>
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Line 14 -->

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Line 21 -->
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!--android:largeHeap="true"-->
        <activity
            android:name="edu.vuum.mocca.ui.ListStoryActivity"
            android:label="@string/title_activity_main" />
        
        <activity android:name="edu.vuum.mocca.ui.LoginActivity">
             <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- Line 35 -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- THIS IS THE CREATION ACTIVITY -->
        <activity android:name="edu.vuum.mocca.ui.CreateStoryActivity" >
        </activity>
        
        <!-- THIS IS THE SINGLE VIEW ACTIVITY -->
        <activity android:name="edu.vuum.mocca.ui.ViewStoryActivity" >
        </activity>
        
        <!-- THIS IS THE EDIT ACTIVITY -->
        <activity android:name="edu.vuum.mocca.ui.EditStoryActivity" > <!-- Line 49 -->
        </activity>

        <!-- THIS IS THE SOUND RECORD ACTIVITY

            android:permission="android.permission.RECORD_AUDIO"

            The name of a permission that clients must have to launch the activity or otherwise get it to respond to an intent.
            If a caller of startActivity() or startActivityForResult() has not been granted the specified permission,
            its intent will not be delivered to the activity.

            VulnCheck app needs to have android.permission.RECORD_AUDIO permission, which has not,
            to be able to be able to send intent to this activity and record audio.
        -->
        <activity
            android:name="edu.vuum.mocca.ui.SoundRecordActivity"
            android:label="@string/title_activity_sound_record"
            android:permission="android.permission.RECORD_AUDIO"
            >
            <intent-filter>                                        <!-- Line 56 -->
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
            </intent-filter>
        </activity>
        
    </application>

</manifest>